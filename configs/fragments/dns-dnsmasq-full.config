# dnsmasq-full（可选 DNS 方案）
#
# 用途：需要 dnsmasq-full 提供的额外特性（例如某些编译选项/功能）。
# 注意：dnsmasq 与 dnsmasq-full 通常互斥，启用 dnsmasq-full 时建议显式禁用 dnsmasq。

CONFIG_PACKAGE_dnsmasq-full=y
# CONFIG_PACKAGE_dnsmasq is not set

# DNS 策略模板依赖（首启由 98-dns-policy 下发 UCI）。
CONFIG_PACKAGE_ip-full=y

