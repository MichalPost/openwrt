# SmartDNS（可选 DNS 方案）
#
# 用途：多上游测速选优、解析加速。
# 注意：建议只启用一套“主 DNS 接管者”，避免与 AdGuardHome / unbound 等同时抢占 53 端口或 DHCP/DNS 劫持链路。

CONFIG_PACKAGE_smartdns=y
CONFIG_PACKAGE_luci-app-smartdns=y
CONFIG_PACKAGE_luci-i18n-smartdns-zh-cn=y

