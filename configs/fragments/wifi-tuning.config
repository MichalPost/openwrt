# Wi-Fi tuning（可选：预设优化所需的最小依赖）
#
# 说明：
# - 具体参数通过 files/etc/uci-defaults/98-wifi-tuning 在首次启动时写入
# - 这里尽量只放轻量、低风险的依赖包，避免替换 wpad/hostapd 变体导致 Wi‑Fi 不起

CONFIG_PACKAGE_iw=y
CONFIG_PACKAGE_wireless-regdb=y

